{{ if .Values.minio.enableServiceMonitors }}
{{ if .Values.minio.serveSingleProtocolPerPod }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minio-http-node-monitor
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  selector:
    matchLabels:
      app: minio-http
  endpoints:
  - port: "8080"
    path: /minio/v2/metrics/node
{{ end }}
{{ end }}
